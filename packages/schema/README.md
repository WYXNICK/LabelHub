# @labelhub/schema

共享 schema 包，当前提供最小阶段 0/规划常量：

- 动态模板 schema
- 数据导入校验 schema

当前导出：

- `templateSchemaVersion`
- `supportedDatasetFormats`

后续阶段补充 API DTO schema、模板 JSON Schema、数据导入校验 schema 和 LLM 结构化输出 schema。任何 schema 变更必须先同步前后端 SDD。
- LLM 结构化输出 schema
